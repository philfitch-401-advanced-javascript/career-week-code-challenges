
const { Set } = require('./Set');

describe('Set', () => {

  let set = new Set;

  it('add', () => {
    set.add('balloon');
    expect(set.array).toEqual(['balloon']);
  })

  it('remove', () => {

  })

  it('has', () => {

  })

  it('intersection', () => {

  })

  it('union', () => {

  })

  it('difference', () => {

  })

  it('static intersection', () => {

  })

  it('static union', () => {

  })

  it('static difference', () => {

  })
})